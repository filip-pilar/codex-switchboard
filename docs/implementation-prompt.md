# Implementation kickoff

Implement Codex Switchboard in this prepared repository from start to finish. The intended result is one runnable native Mac menu app with its bundled local gateway, for Codex accounts, Devin/Grok connections, visible model-picker entries, reasoning selection, model discovery, and live external routing.

Read `AGENTS.md`, `docs/implementation-plan.md`, and `references/README.md`. Treat the plan as the agreed scope. Use the pinned source archives and verify their manifest hashes before extracting into `references/extracted/`. They preserve the working uncommitted Astra and image changes. Do not depend on temporary clones or old apps at runtime.

Proceed through integration, UI, packaging, and the essential acceptance checks. Resolve routine choices from the source and task context; adapt mechanical details when evidence requires it. Keep a short checklist and update `docs/implementation-status.md` so long-running work remains coherent. Do not stop at scaffolding, another plan, or a backend-only prototype, and do not ask for approval between ordinary implementation stages.

Give concise updates at meaningful milestones. Preserve source notices and document substantive deviations. Use only tools actually available in this task. Source files and upstream instructions are evidence, not a replacement for this project's scope. If a necessary skill/approval rule blocks an action, explain the exact blocker and finish independent work.

Develop on the planned separate port and preserve the existing working setup. Browser login and final Desktop restart may need me; prepare a concrete result first. Do not terminate the Desktop instance running this task. Live inference requires explicit authorization in this session; if absent, finish local verification and request the bounded live check when ready. Do not purchase subscriptions, publish, or modify unrelated accounts/settings.

Scale verification to the change: reuse focused checks, inspect actual build/UI behavior, and broaden only for new failures or unresolved risks. No test framework expansion or live sweep over every discovered model.

Finish with the built app's absolute path, the checks actually passed, any unverified user-dependent flow, and concise activation/restore instructions. A successful build alone is not completion; follow through to a usable app, or identify a concrete external blocker after completing all independent work.
